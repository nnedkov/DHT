High priority:

	ELIAS:
		* complete functionality for ping - find_node - find_value - verify in file kademlia_protocol_server.py
		* Comments: - test your added functionality by adding test code under "if __name__ == '__main__'", check example
					- run tests by executing: python kademlia_protocol_server.py
					- don't forger to follow the defined git workflow (git_workflow.txt)
		* create k-buckets data structure, provide CRUD (create-read-update-delete) operations API

	NEDKO:
		* seperate modules/refactoring
		* write functionality for DHTAPIServer (KX request parsing - handling - responding)



Low priority:
* bash script that installs all required packages

