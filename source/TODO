High priority:

	ELIAS:
		* complete functionality for ping - find_node - find_value - verify in file kademlia_protocol_server.py
		* add functionality in maintain_til_shutdown function (maintenance of buckets)

	NEDKO:
		* set timeout in ping, store (and generally anything that deals with sockets)
		* sending the key through socket as a string or as bits? check: http://stackoverflow.com/questions/142812/does-python-have-a-bitfield-type
		* servers don't bind the correct port
		* reserved bits
		* TUM Informatics: ask for testing code

Low priority:
* bash script that installs all required packages

